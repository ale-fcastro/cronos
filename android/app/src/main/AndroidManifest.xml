<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Uso del teléfono (pestaña Analizar > Teléfono, vincular tareas con
         apps): permiso especial, el usuario lo concede desde Configuración
         del sistema via UsageStats.grantUsagePermission(), no con un diálogo
         normal. -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions"/>
    <!-- Recordatorios de tareas planificadas (flutter_local_notifications).
         POST_NOTIFICATIONS ya lo declara el propio plugin; esto habilita
         que los avisos agendados sobrevivan a un reinicio del teléfono. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Foto de perfil opcional (image_picker): la cámara se declara pero
         no es requerida, para no excluir dispositivos sin una. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <!-- Chequeo de actualizaciones contra GitHub Releases. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Instalar el APK descargado dentro de la app (Android 8+ pide este
         permiso especial para lanzar el instalador desde una app propia). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <!-- Notificación persistente del cronómetro de sesión (SessionForegroundService):
         mantiene el proceso en foreground mientras hay una tarea/actividad
         corriendo, para que el sistema no la trate como "en background". -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <application
        android:label="Cronos"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Widget "Hoy": score, tiempo productivo/perdido, próxima tarea.
             lib/core/services/home_widget_service.dart lo dispara vía
             home_widget.updateWidget(qualifiedAndroidName: "...HomeWidgetProvider"). -->
        <receiver android:name=".widgets.HomeWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/home_widget_info"/>
        </receiver>

        <!-- Widget "Sesión activa": tarea/actividad en curso con Pausar/Finalizar. -->
        <receiver android:name=".widgets.SessionWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/session_widget_info"/>
        </receiver>

        <!-- Widget "Semanal": una barra por día con el score de esta semana. -->
        <receiver android:name=".widgets.WeeklyWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/weekly_widget_info"/>
        </receiver>

        <!-- Widget "Hábitos": check diario + racha. -->
        <receiver android:name=".widgets.HabitsWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/habits_widget_info"/>
        </receiver>

        <!-- Notificación persistente de sesión: lib/core/services/session_notification_service.dart
             la arranca/detiene vía el canal cronos/session_service (ver MainActivity.kt). -->
        <service android:name=".SessionForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="time_tracking"/>
        </service>

        <!-- Detección automática de contexto por app (Configuración > App Tracking):
             sondea la app en primer plano mientras el usuario la tenga activada. -->
        <service android:name=".AppTrackingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="app_usage_tracking"/>
        </service>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: abrir el navegador para descargar el APK del release. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <!-- Visibilidad de paquetes (Android 11+): sin esto,
             PackageManager.queryIntentActivities en MainActivity.getInstalledApps()
             solo veía un puñado de apps, la mayoría quedaban invisibles para
             Cronos por default. Necesario para que el selector de "apps
             vinculadas" de App Tracking (y de una categoría de actividad)
             liste TODAS las apps con ícono en el launcher, igual que ya podía
             ver AppUsageService.queryUsage vía el permiso especial de
             "Acceso al uso" (ese no está sujeto a este filtro). -->
        <intent>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>
    </queries>
</manifest>
